<article class="post">
  <h2>{{ post.title }}</h2>
  {{!-- <address>Posted by {{post.poster.name}}</address> --}}
  <p class="author">Posted by <a href="/users/{{poster._id}}">{{poster.userName}}</a></p>
  <p>{{ post.body }}</p>

  {{#if post.tags.length}}
    <h3>Tags</h3>
    <ul>
      {{#each post.tags}}
        <li><a href="/posts/tag/{{this}}">{{this}}</a></li>
      {{/each}}
    </ul>
  {{/if}}
  {{#if post.images.length}}
    <h3>Images</h3>
    <ul>
      {{#each post.images}}
        <li><img src="/public/images/{{this}}.jpg" alt={{this}} class='picture'></li>
      {{/each}}
    </ul>
  {{/if}}
  {{#if post.replies.length}}
    <h3>Replies</h3>
    <ul id="reply-list">
      {{#each replies}}
      <li><a href="/posts/replies/{{_id}}">{{text}}</a></li>
        <li>
          <ul>
            <li>Posted by <a href="/users/{{replier._id}}">{{replier.userName}}</a></li>
          </ul>
        </li>
      {{/each}}
    </ul>
  {{/if}}

<!--
  <form action="/replies" method="POST" class="reply-form">
  <input type="hidden" name="id" value={{post._id}} placeholder="text" class="full-width-input" />
    <label>
      reply
      <input type="text" name="text" placeholder="text" class="full-width-input" />
      
    </label>
    <br>    
    <button type="submit">submit</button>
</form>
/!-->
      <form id="reply-submit">
        <input type="hidden" id="post-id" name="id" value={{post._id}} />
        <label for="post-body">
          Reply Body
        </label>
        <textarea id="post-body" name="body" rows="4" placeholder="Reply Content" class="full-width-input">{{post.body}}</textarea>
        <br>  
        <button type="submit">Submit</button>
      </form>
  {{#if hasErrors}}
  <ul class="error-list">
    {{#each errors}}
    <li>
      {{this}}
    </li>
    {{/each}}
  </ul>
  {{/if}}
  <a href="/posts">Back to All Posts</a>
</article>
